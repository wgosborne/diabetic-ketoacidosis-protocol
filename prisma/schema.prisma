generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model room {
  id      Int  @id @default(autoincrement())
  step    Int?
  roomNum Int?
}
